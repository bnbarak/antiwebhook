# Flask Test App

Test simplehook with a real Flask server.

```bash
pip install flask websockets
SIMPLEHOOK_KEY=ak_your_key python app.py
```

Webhooks sent to your simplehook URL will appear in the console.
