# React Live Webhook Monitor

A real-time dashboard that displays webhooks as they arrive via simplehook.

Handles Stripe, GitHub, Twilio, and Shopify webhooks with provider-specific responses.

```bash
npm install
SIMPLEHOOK_KEY=ak_your_key npm run dev
```

Open http://localhost:3002 to see webhooks arrive in real-time.
